# Horizon Analytics (MVP)

AI-first analytics for uploaded tables.

## Run locally

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python chainlit run app.py -w